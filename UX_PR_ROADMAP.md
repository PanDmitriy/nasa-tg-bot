# План Pull Request'ов для улучшения UX

**Цель:** Реализовать улучшения UX согласно приоритетам из UX_AUDIT.md  
**Приоритеты:** 🟢 Срочно и 🟡 Средний приоритет  
**Оценка:** 3-5 дней разработки

---

## 📋 Общая стратегия

PR'ы сгруппированы логически для минимизации конфликтов и максимизации независимости работы. Каждый PR должен быть:
- ✅ Независимым (может быть смержен отдельно)
- ✅ Протестированным (все изменения проверены)
- ✅ Документированным (описание изменений в PR)

---

## 🟢 Срочно (критично для пользовательского опыта)

### PR #1: Улучшение сообщений об ошибках

**Название:** `feat(ux): улучшить сообщения об ошибках для пользователей`

**Описание:**
Убрать технические детали из сообщений об ошибках и добавить понятные объяснения с предложениями действий.

**Файлы для изменения:**
- `src/shared/lib/errorHandler/errorHandler.ts`
- `src/processes/bot/handlers/earth.ts` (улучшить существующие сообщения)

**Изменения:**
1. Улучшить `handleTelegramError` — убрать коды ошибок (503, 429, 5xx) из сообщений
2. Добавить понятные объяснения для каждого типа ошибки
3. Добавить предложения альтернативных действий
4. Добавить кнопки навигации при ошибках (где уместно)

**Примеры изменений:**
```typescript
// Было:
await ctx.reply('⚠️ Сервис NASA временно недоступен (5xx). Попробуйте позже.');

// Стало:
await ctx.reply(
  '❌ <b>Не удалось получить данные</b>\n\n' +
  'Похоже, сервер NASA временно недоступен. Это случается редко и обычно решается за несколько минут.\n\n' +
  '💡 <b>Что можно сделать:</b>\n' +
  '• Подождать 1-2 минуты и попробовать снова\n' +
  '• Попробовать другую команду (например, /images)\n' +
  '• Вернуться позже',
  {
    parse_mode: 'HTML',
    ...Markup.inlineKeyboard([
      [Markup.button.callback('🔄 Повторить', 'earth_retry')],
      [Markup.button.callback('🖼️ Галерея', 'images_menu')],
      [Markup.button.callback('🏠 Меню', 'main_menu')]
    ])
  }
);
```

**Тестирование:**
- Проверить все типы ошибок (timeout, 429, 5xx, общие)
- Убедиться, что кнопки работают корректно

**Зависимости:** Нет

---

### PR #2: Улучшение приветственного сообщения

**Название:** `feat(ux): улучшить приветственное сообщение с эмоциональной составляющей`

**Описание:**
Сделать приветственное сообщение более вдохновляющим и информативным, добавить inline-кнопки для быстрого доступа к командам.

**Файлы для изменения:**
- `src/processes/bot/handlers/start.ts`

**Изменения:**
1. Переписать текст приветствия — добавить эмоциональную составляющую
2. Добавить inline-кнопки с популярными командами
3. Улучшить структуру сообщения

**Пример изменений:**
```typescript
const message = `🌌 <b>Добро пожаловать в космическое путешествие!</b>\n\n` +
  `Привет! Я твой персональный гид по Вселенной. Каждый день NASA открывает что-то удивительное — и я покажу тебе это первым.\n\n` +
  `✨ <b>Что я умею:</b>\n` +
  `• Показывать потрясающие фото космоса каждый день\n` +
  `• Отслеживать космическую погоду в реальном времени\n` +
  `• Находить астероиды, приближающиеся к Земле\n` +
  `• И многое другое!\n\n` +
  `🚀 <b>Начни исследование:</b>`;

const keyboard = Markup.inlineKeyboard([
  [
    Markup.button.callback('🌌 Фото дня', 'quick_apod'),
    Markup.button.callback('🌍 Земля', 'quick_earth')
  ],
  [
    Markup.button.callback('☄️ Астероиды', 'quick_asteroids'),
    Markup.button.callback('🌊 Косм. погода', 'quick_donki')
  ],
  [
    Markup.button.callback('🖼️ Галерея', 'quick_images'),
    Markup.button.callback('📋 Все команды', 'help_menu')
  ]
]);

await ctx.reply(message, { parse_mode: 'HTML', ...keyboard });
```

**Дополнительно:**
- Добавить обработчики для quick-кнопок (они должны вызывать соответствующие команды)

**Тестирование:**
- Проверить отображение сообщения
- Проверить работу всех кнопок
- Проверить на мобильных устройствах

**Зависимости:** Нет (но может использовать главное меню из PR #3)

---

### PR #3: Добавление главного меню

**Название:** `feat(ux): добавить главное меню для упрощения навигации`

**Описание:**
Создать универсальное главное меню, доступное из любой точки бота, для упрощения навигации.

**Файлы для изменения:**
- `src/processes/bot/handlers/main-menu.ts` (новый файл)
- `src/processes/bot/index.ts` (добавить обработчик)
- Все существующие обработчики (добавить кнопку "🏠 Меню" где уместно)

**Изменения:**
1. Создать новый handler `handleMainMenu`
2. Добавить обработчик callback `main_menu` в `bot/index.ts`
3. Добавить кнопку "🏠 Меню" в существующие сообщения (APOD, Earth, Images, DONKI, Asteroids)
4. Создать утилиту для генерации клавиатуры главного меню

**Пример кода:**
```typescript
// src/processes/bot/handlers/main-menu.ts
import { Context, Markup } from 'telegraf';
import { BotContext } from '../types';

export async function handleMainMenu(ctx: Context & BotContext) {
  const message = `📋 <b>Главное меню</b>\n\n` +
    `Выберите интересующую команду:`;

  const keyboard = Markup.inlineKeyboard([
    [
      Markup.button.callback('🌌 Фото дня', 'quick_apod'),
      Markup.button.callback('🌍 Земля', 'quick_earth')
    ],
    [
      Markup.button.callback('☄️ Астероиды', 'quick_asteroids'),
      Markup.button.callback('🖼️ Галерея', 'quick_images')
    ],
    [
      Markup.button.callback('🌊 Косм. погода', 'quick_donki'),
      Markup.button.callback('📅 Подписки', 'quick_subscribe')
    ],
    [
      Markup.button.callback('❓ Помощь', 'help_menu'),
      Markup.button.callback('⚙️ Настройки', 'settings_menu')
    ]
  ]);

  try {
    await ctx.editMessageText(message, { parse_mode: 'HTML', ...keyboard });
  } catch {
    await ctx.reply(message, { parse_mode: 'HTML', ...keyboard });
  }
}

// Утилита для добавления кнопки меню
export function addMainMenuButton(keyboard: any) {
  // Добавляет кнопку "🏠 Меню" к существующей клавиатуре
  if (!keyboard.inline_keyboard) {
    keyboard.inline_keyboard = [];
  }
  keyboard.inline_keyboard.push([
    { text: '🏠 Меню', callback_data: 'main_menu' }
  ]);
  return keyboard;
}
```

**Интеграция в существующие handlers:**
- `apod.ts` — добавить кнопку после отправки фото
- `earth.ts` — добавить кнопку после отправки снимка
- `images.ts` — добавить кнопку в навигацию
- `donki.ts` — добавить кнопку в меню
- `asteroids.ts` — добавить кнопку после отправки данных

**Тестирование:**
- Проверить отображение меню
- Проверить работу всех кнопок
- Проверить интеграцию во все handlers

**Зависимости:** Нет

---

### PR #4: Улучшение APOD с навигацией и "Читать полностью"

**Название:** `feat(ux): добавить навигацию и кнопку "Читать полностью" в APOD`

**Описание:**
Добавить кнопки навигации после просмотра APOD и возможность прочитать полное описание.

**Файлы для изменения:**
- `src/processes/bot/handlers/apod.ts`
- `src/features/apod/services/apodService.ts`
- `src/processes/bot/index.ts` (добавить обработчики callback)

**Изменения:**
1. Добавить кнопки навигации после отправки APOD:
   - "🌌 Еще фото" — получить другое случайное фото
   - "📖 Читать полностью" — показать полное описание
   - "🔗 На сайте NASA" — ссылка на оригинал
   - "🏠 Меню" — главное меню
2. Создать handler для "Читать полностью" — отправлять полное описание
3. Улучшить форматирование — сократить описание до 300 символов в подписи
4. Добавить обработчик для "Еще фото"

**Пример кода:**
```typescript
// В apod.ts после отправки фото
const caption = container.apodService.formatApodAsImage(apod);
const keyboard = Markup.inlineKeyboard([
  [
    Markup.button.callback('📖 Читать полностью', `apod_full_${apod.date}`),
    Markup.button.callback('🔗 На сайте NASA', 'apod_nasa_link')
  ],
  [
    Markup.button.callback('🌌 Еще фото', 'apod_random'),
    Markup.button.callback('🏠 Меню', 'main_menu')
  ]
]);

await ctx.replyWithPhoto(apod.url, {
  caption,
  parse_mode: 'HTML',
  ...keyboard
});

// Новый handler для полного описания
export async function handleApodFull(ctx: Context & BotContext) {
  const data = getCallbackQueryData(ctx);
  const date = data.replace('apod_full_', '');
  
  try {
    await ctx.answerCbQuery('📖 Загружаю полное описание...');
    const apod = await container.apodService.getApod(date);
    
    const fullMessage = `🌌 <b>${apod.title}</b>\n\n` +
      `📅 <i>${new Date(apod.date).toLocaleString('ru-RU')}</i>\n\n` +
      `${apod.explanation}\n\n` +
      `📸 <i>NASA Astronomy Picture of the Day</i>\n\n` +
      `🔗 <a href="${apod.url}">Открыть на сайте NASA</a>`;
    
    const keyboard = Markup.inlineKeyboard([
      [Markup.button.callback('🌌 Еще фото', 'apod_random')],
      [Markup.button.callback('🏠 Меню', 'main_menu')]
    ]);
    
    await ctx.reply(fullMessage, {
      parse_mode: 'HTML',
      ...keyboard,
      link_preview_options: { is_disabled: true }
    });
  } catch (error) {
    await handleTelegramError(ctx, error, 'ApodFull');
  }
}
```

**Обновить formatApodAsImage:**
```typescript
formatApodAsImage(apod: ApodResponse): string {
  const shortExplanation = apod.explanation.length > 300
    ? apod.explanation.substring(0, 300) + '...'
    : apod.explanation;
    
  return `🌌 <b>${apod.title}</b>\n\n` +
    `📅 <i>${new Date(apod.date).toLocaleString('ru-RU')}</i>\n\n` +
    `${shortExplanation}\n\n` +
    `📸 <i>NASA Astronomy Picture of the Day</i>`;
}
```

**Тестирование:**
- Проверить отображение кнопок
- Проверить работу "Читать полностью"
- Проверить работу "Еще фото"
- Проверить работу ссылки на NASA

**Зависимости:** PR #3 (главное меню)

---

## 🟡 Средний приоритет (важно для качества)

### PR #5: Интерактивный /help

**Название:** `feat(ux): сделать /help интерактивным с меню команд`

**Описание:**
Заменить длинный текст в /help на интерактивное меню с возможностью получить информацию о каждой команде отдельно.

**Файлы для изменения:**
- `src/processes/bot/handlers/help.ts`
- `src/processes/bot/index.ts` (добавить обработчики)

**Изменения:**
1. Переписать `handleHelp` — показывать меню вместо длинного текста
2. Создать отдельные handlers для каждой команды в help:
   - `handleHelpApod` — информация об APOD
   - `handleHelpEarth` — информация о Earth
   - `handleHelpAsteroids` — информация об астероидах
   - `handleHelpImages` — информация о галерее
   - `handleHelpDonki` — информация о DONKI
   - `handleHelpSubscriptions` — информация о подписках
3. Добавить кнопку "Попробовать" в каждом разделе help

**Пример кода:**
```typescript
export async function handleHelp(ctx: Context & BotContext) {
  const message = `📚 <b>Помощь</b>\n\n` +
    `Выберите интересующую тему или команду:\n\n` +
    `<b>Основные команды:</b>`;

  const keyboard = Markup.inlineKeyboard([
    [
      Markup.button.callback('🌌 APOD', 'help_apod'),
      Markup.button.callback('🌍 Земля', 'help_earth')
    ],
    [
      Markup.button.callback('☄️ Астероиды', 'help_asteroids'),
      Markup.button.callback('🖼️ Галерея', 'help_images')
    ],
    [
      Markup.button.callback('🌊 DONKI', 'help_donki'),
      Markup.button.callback('📅 Подписки', 'help_subscriptions')
    ],
    [
      Markup.button.callback('❓ Общие вопросы', 'help_general'),
      Markup.button.callback('🏠 Меню', 'main_menu')
    ]
  ]);

  await ctx.reply(message, { parse_mode: 'HTML', ...keyboard });
}

export async function handleHelpApod(ctx: Context & BotContext) {
  const message = `🌌 <b>APOD (Astronomy Picture of the Day)</b>\n\n` +
    `Получите потрясающее фото космоса от NASA каждый день!\n\n` +
    `<b>Как использовать:</b>\n` +
    `• Просто отправьте: <code>/apod</code>\n` +
    `• Бот покажет случайное фото из архива NASA\n\n` +
    `<b>Примеры:</b>\n` +
    `• <code>/apod</code> — получить случайное фото\n\n` +
    `💡 <b>Совет:</b> Подпишитесь на ежедневные уведомления командой /subscribe`;

  const keyboard = Markup.inlineKeyboard([
    [Markup.button.callback('🚀 Попробовать', 'quick_apod')],
    [Markup.button.callback('⬅️ Назад к помощи', 'help_menu')]
  ]);

  try {
    await ctx.editMessageText(message, { parse_mode: 'HTML', ...keyboard });
  } catch {
    await ctx.reply(message, { parse_mode: 'HTML', ...keyboard });
  }
}
```

**Тестирование:**
- Проверить отображение главного меню help
- Проверить информацию о каждой команде
- Проверить работу кнопки "Попробовать"
- Проверить навигацию назад

**Зависимости:** PR #3 (главное меню)

---

### PR #6: Добавление персонализации

**Название:** `feat(ux): добавить персонализацию с использованием имени пользователя`

**Описание:**
Использовать имя пользователя в сообщениях для создания более персонального опыта.

**Файлы для изменения:**
- `src/processes/bot/handlers/start.ts`
- `src/processes/bot/handlers/apod.ts`
- `src/shared/lib/telegramHelpers.ts` (добавить утилиту для получения имени)

**Изменения:**
1. Создать утилиту `getUserName(ctx)` для получения имени пользователя
2. Использовать имя в приветственном сообщении
3. Использовать имя в APOD (опционально, не всегда)
4. Добавить персонализацию в другие команды (где уместно)

**Пример кода:**
```typescript
// src/shared/lib/telegramHelpers.ts
export function getUserName(ctx: Context & BotContext): string {
  if (ctx.from?.first_name) {
    return ctx.from.first_name;
  }
  if (ctx.from?.username) {
    return ctx.from.username;
  }
  return 'друг';
}

// В start.ts
export async function handleStart(ctx: BotContext) {
  const userName = getUserName(ctx);
  
  const message = `🌌 <b>Добро пожаловать в космическое путешествие!</b>\n\n` +
    `Привет, ${userName}! 👋 Я твой персональный гид по Вселенной. Каждый день NASA открывает что-то удивительное — и я покажу тебе это первым.\n\n` +
    // ... остальной текст
}
```

**Тестирование:**
- Проверить отображение имени в разных случаях (есть first_name, только username, нет ничего)
- Проверить на разных языках
- Убедиться, что персонализация не навязчива

**Зависимости:** Нет

---

### PR #7: Улучшение форматирования длинных сообщений

**Название:** `feat(ux): улучшить форматирование длинных сообщений с разделителями`

**Описание:**
Добавить визуальные разделители и структуру в длинные сообщения (DONKI, астероиды) для улучшения читаемости.

**Файлы для изменения:**
- `src/features/donki/formatters.ts`
- `src/processes/bot/handlers/asteroids.ts`
- `src/features/asteroids/services/asteroidsService.ts`

**Изменения:**
1. Добавить разделители (`─────────────────────`) в DONKI форматтеры
2. Улучшить структуру сообщений об астероидах
3. Добавить визуальное разделение секций
4. Сократить избыточную информацию в подробном режиме DONKI

**Пример кода:**
```typescript
// В donki/formatters.ts - formatCMESimple
export function formatCMESimple(cme: DonkiCME): string {
  // ... существующий код ...
  
  let text = `${risk.emoji} <b>Солнечный шторм</b>\n\n`;
  text += `📅 <b>Когда:</b> ${time}\n`;
  text += `${risk.emoji} <b>Уровень:</b> ${risk.description}\n`;
  text += `⚡ <b>Скорость:</b> ${Math.round(speed)} км/с\n\n`;
  text += `─────────────────────\n\n`;
  text += `💡 <b>Что это значит:</b>\n\n`;
  // ... объяснение ...
  text += `\n─────────────────────\n\n`;
  text += `⏱️ <b>Ожидаемое прибытие:</b> через ${arrivalEstimate}\n`;
  
  return text;
}
```

**Для астероидов:**
```typescript
// Улучшить структуру сообщения
const message = `☄️ <b>Информация об астероидах</b>\n` +
  `<i>За последние 7 дней</i>\n\n` +
  `─────────────────────\n\n` +
  `⚠️ <b>Потенциально опасных:</b> ${hazardous.length}\n` +
  `🟢 <b>Ближайших безопасных:</b> ${sortedNonHazardous.length} из ${nonHazardous.length}\n\n` +
  `─────────────────────\n\n` +
  // ... список астероидов с разделением
```

**Тестирование:**
- Проверить читаемость длинных сообщений
- Проверить на мобильных устройствах
- Убедиться, что разделители не перегружают сообщение

**Зависимости:** Нет

---

### PR #8: Индикация прогресса в подписках

**Название:** `feat(ux): добавить индикацию прогресса в многошаговых процессах подписок`

**Описание:**
Показывать пользователю, на каком шаге он находится в процессе подписки (1/3, 2/3, 3/3).

**Файлы для изменения:**
- `src/features/subscriptions/commands.subscribe.ts`

**Изменения:**
1. Добавить индикацию прогресса в каждый шаг подписки
2. Обновить сообщения с указанием шага
3. Добавить визуальный индикатор (например, прогресс-бар эмодзи)

**Пример кода:**
```typescript
// Шаг 1
const message = `📅 <b>Подписка на Daily APOD</b>\n` +
  `Шаг 1 из 3\n\n` +
  `Выберите тип подписки:`;

// Шаг 2
const message = `📅 <b>Подписка: ${typeNames[type]}</b>\n` +
  `Шаг 2 из 3\n\n` +
  `Выберите время доставки (UTC):`;

// Шаг 3
const message = `📅 <b>Подтверждение подписки</b>\n` +
  `Шаг 3 из 3\n\n` +
  // ... остальной текст
```

**Визуальный индикатор:**
```typescript
function getProgressIndicator(step: number, total: number): string {
  const filled = '🟩'.repeat(step);
  const empty = '⬜'.repeat(total - step);
  return `${filled}${empty} ${step}/${total}`;
}

// Использование:
const progress = getProgressIndicator(1, 3); // "🟩⬜⬜ 1/3"
```

**Тестирование:**
- Проверить отображение прогресса на всех шагах
- Проверить визуальный индикатор
- Убедиться, что прогресс понятен пользователю

**Зависимости:** Нет

---

### PR #9: Улучшение сообщений об отсутствии данных

**Название:** `feat(ux): улучшить сообщения об отсутствии данных с объяснениями и предложениями`

**Описание:**
Вместо простого "Не найдено" объяснять, почему данных нет, и предлагать альтернативные действия.

**Файлы для изменения:**
- `src/processes/bot/handlers/donki.ts` (все handlers с проверкой на пустой результат)
- `src/processes/bot/handlers/images.ts`
- `src/processes/bot/handlers/asteroids.ts`

**Изменения:**
1. Улучшить сообщения "не найдено" в DONKI handlers
2. Улучшить сообщения в Images при пустом результате поиска
3. Улучшить сообщения в Asteroids (если нет астероидов)
4. Добавить кнопки с предложениями альтернативных действий

**Пример кода:**
```typescript
// В donki.ts - handleDonkiCMEData
if (cmes.length === 0) {
  const message = `🌊 <b>Нет событий CME</b>\n\n` +
    `За выбранный период не было зарегистрировано корональных выбросов массы.\n\n` +
    `☀️ <b>Это хорошая новость!</b> Космическая погода спокойная, и это означает стабильные условия для спутников и космических миссий.\n\n` +
    `💡 <b>Попробуйте:</b>\n` +
    `• Выбрать другой период (неделя, месяц)\n` +
    `• Посмотреть другие типы событий (вспышки, геобури)\n` +
    `• Подписаться на уведомления о новых событиях`;
  
  const keyboard = Markup.inlineKeyboard([
    [
      Markup.button.callback('📅 Другой период', 'donki_cme'),
      Markup.button.callback('☀️ Вспышки', 'donki_flares')
    ],
    [
      Markup.button.callback('🌍 Геобури', 'donki_gst'),
      Markup.button.callback('🔔 Подписаться', 'donki_subscriptions')
    ],
    [Markup.button.callback('🔙 Назад', 'donki_menu')]
  ]);
  
  await ctx.editMessageText(message, {
    parse_mode: 'HTML',
    ...keyboard
  });
  return;
}
```

**Для Images:**
```typescript
if (images.length === 0) {
  const message = `❌ <b>Изображения не найдены</b>\n\n` +
    `По запросу "<i>${query}</i>" изображений не найдено.\n\n` +
    `💡 <b>Советы для поиска:</b>\n` +
    `• Используйте английские слова (Mars, Apollo, Hubble)\n` +
    `• Попробуйте более общие термины\n` +
    `• Выберите тему из популярных категорий`;
  
  const keyboard = Markup.inlineKeyboard([
    [Markup.button.callback('📋 Популярные темы', 'images_menu')],
    [Markup.button.callback('🔍 Новый поиск', 'images_custom_search')],
    [Markup.button.callback('🏠 Меню', 'main_menu')]
  ]);
  
  await ctx.reply(message, { parse_mode: 'HTML', ...keyboard });
  return;
}
```

**Тестирование:**
- Проверить все случаи "не найдено" в разных командах
- Проверить работу предложенных кнопок
- Убедиться, что сообщения понятны и полезны

**Зависимости:** PR #3 (главное меню)

---

## 📊 Последовательность выполнения

### Фаза 1: Критичные улучшения (🟢 Срочно)

1. **PR #1** — Улучшение сообщений об ошибках (независимо)
2. **PR #2** — Улучшение приветствия (независимо)
3. **PR #3** — Главное меню (независимо, но используется в других PR)
4. **PR #4** — Улучшение APOD (зависит от PR #3)

**Порядок мержа:**
- PR #1 → PR #2 → PR #3 → PR #4

### Фаза 2: Качественные улучшения (🟡 Средний приоритет)

5. **PR #5** — Интерактивный /help (зависит от PR #3)
6. **PR #6** — Персонализация (независимо)
7. **PR #7** — Форматирование сообщений (независимо)
8. **PR #8** — Индикация прогресса (независимо)
9. **PR #9** — Сообщения об отсутствии данных (зависит от PR #3)

**Порядок мержа:**
- PR #6, PR #7, PR #8 (можно параллельно)
- PR #5 (после PR #3)
- PR #9 (после PR #3)

---

## ✅ Чеклист для каждого PR

Перед созданием PR убедитесь:

- [ ] Код соответствует стилю проекта
- [ ] Все изменения протестированы вручную
- [ ] Добавлены/обновлены комментарии где необходимо
- [ ] Нет конфликтов с основной веткой
- [ ] Описание PR содержит:
  - Что изменено
  - Зачем изменено
  - Как протестировать
  - Скриншоты (если применимо)
- [ ] Все новые обработчики добавлены в `bot/index.ts`
- [ ] Проверено на мобильных устройствах (если применимо)

---

## 🧪 Общие рекомендации по тестированию

1. **Тестирование на разных устройствах:**
   - Telegram Desktop
   - Telegram Mobile (Android/iOS)
   - Telegram Web

2. **Тестирование сценариев:**
   - Первое использование бота
   - Повторное использование
   - Обработка ошибок
   - Навигация между командами

3. **Проверка граничных случаев:**
   - Пустые результаты
   - Ошибки API
   - Длинные тексты
   - Специальные символы в именах

---

## 📝 Примечания

- Все PR должны быть небольшими и фокусированными на одной задаче
- Если PR становится слишком большим (>500 строк изменений), стоит разбить его на несколько PR
- После мержа каждого PR стоит проверить, что ничего не сломалось
- Рекомендуется использовать feature-ветки: `feature/ux-improve-error-messages`, `feature/ux-main-menu`, и т.д.

---

**Автор плана:** UX/UI Designer & Product Analyst  
**Дата создания:** 2025-01-XX  
**Статус:** Готов к реализации

